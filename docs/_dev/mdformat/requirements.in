# Direct dependencies for the documentation Markdown formatter.
#
# Edit this file to change formatter versions, then regenerate the
# hash-pinned lock file:
#
#     make -C docs mdformat-lock
#
# Commit both files together.

mdformat==1.0.0
mdformat-myst==0.3.0