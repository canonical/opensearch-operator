#
# Constraints applied during charmcraft builds.
#
# charmcraft installs dependencies from source and some build backends (e.g. older setuptools_scm)
# still import `pkg_resources`. Ensure we use a setuptools version that bundles it.
#
# setuptools 69.x trips on some packages' PEP 621 license metadata (e.g. google-crc32c),
# while very new setuptools versions (>=82) remove `pkg_resources` which older build
# backends still import (e.g. setuptools_scm 3.x). Pin to a compatible middle ground.
setuptools==80.9.0

