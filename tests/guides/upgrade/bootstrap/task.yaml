summary: "Upgrade guide bootstrap: environment and baseline deployment"
priority: 900
kill-timeout: 90m
environment:
  HOME: /root
prepare: |
  export HOME=/root
  mkdir -p /root/.local/share/juju
execute: |
  bash "$SPREAD_PATH/$SPREAD_TASK.sh"
